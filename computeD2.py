import numpy as np


def calc_access_delay_vect(message_dict_array):
    # for every message calculates the media access delay
    # d2 = WCRT - d3

    # calculate WCRT for every message iteratively
    for message_dict in message_dict_array:
        while () and ():